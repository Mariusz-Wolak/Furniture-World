<aura:component description="FW_ProductsPricebookResult">
    <aura:attribute name="pricebook" type="Object" access="global"/>

    <aura:registerEvent name="FW_SelectedPricebook" type="c:FW_SelectedPricebook"/>

<!--    class="{!concat('photoDiv', if(v.photoObject.isSelected, ' bordered', ''))}"-->
    <div class="resultDiv slds-grid" onclick="{!c.selectPricebook}">
        <div class="pricebookName slds-col slds-size_4-of-12">
                {!v.pricebook.Name}
        </div>

        <div class="slds-col slds-size_3-of-12">
                {!v.pricebook.StartDate__c}
        </div>

        <div class="slds-col slds-size_3-of-12">
                {!v.pricebook.EndDate__c}
        </div>

        <div class="slds-col slds-size_1-of-12">
            <lightning:icon aura:id="removeIcon" class="removeIcon" iconName="action:more" alternativeText="{!$Label.c.Remove}" title="{!$Label.c.Remove}"
                            size="xx-small" onclick="{!c.removeFromPricebook}"/>
        </div>
    </div>
</aura:component>
