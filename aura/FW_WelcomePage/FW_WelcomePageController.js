({
    scrollDown: function(component, event, helper){
        let scrollOptions = {
            left: 0,
            top: 1100,
            behavior: 'smooth'
        }
        window.scrollTo(scrollOptions);
    }
})